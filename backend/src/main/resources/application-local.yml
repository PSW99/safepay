# JWT
jwt:
  secret: safepay-jwt-secret-key-for-development-only-change-in-production-please

# Encryption
encryption:
  aes-key: safepay-aes-256-secret!

# Logging
logging:
  level:
    com.safepay: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
